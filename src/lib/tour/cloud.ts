import type { TourStep } from './types'

/**
 * 「云过审」页新手教程。
 *
 * ══ 主线 ══════════════════════════════════════════════════════════════
 * 这页不是「填个表单就完事」，它是**给客户端加一层本地改写代理**，所以第 1 步
 * 必须先把「为什么需要它」讲透：客户端直连中转站时，提示词会被云端审查拦下、
 * 直接回拒答。云过审在本机监听一个地址，客户端把 base_url 指过来，请求先被
 * 改写（洗白 / 令牌化 / 预热）再转发上游，响应回来后判定 / 还原 / 洗白才回传。
 *
 * 之后按真实使用顺序走：看服务端 → 启动 → 读统计 → 填上游 → 认规则与开关 →
 * 看日志 → 接客户端 → 保存 / 还原默认 → 收尾。不按界面从左到右讲，因为那样会把
 * 「填上游 → 启动 → 接客户端」这条主线拆成三段，用户记不住。
 *
 * 两个位置上的取舍：
 *   · 统计那一行在 SERVER 面板里，紧跟着「启动」讲完，不单独拖到后面 ——
 *     否则视线要在两块面板之间来回跳。
 *   · RULES 面板先整块讲（规则从哪来），再把镜头推进到里面的开关行，
 *     跟「目标」页「先面板后内部」的顺序一致。
 *
 * ══ 为什么整组步骤一个 before 都没有 ═════════════════════════════════
 * `before` 用于展开折叠面板 / 切子标签 / 选中某一行。这一页的四个面板
 * （SERVER / UPSTREAM / RULES / LOG）全部无条件渲染，没有需要先点开的东西；
 * 页面上能点开的只有「规则表文件」（弹窗编辑器）以及「启动服务端 / 中转站自检 /
 * 保存 / 还原默认 / 清零」这类**会起服务或会落盘**的按钮 —— 教程一步都不碰。
 * 所以这组步骤只高亮讲解，不做任何准备动作。
 *
 * 锚点：`cloud.*` 全部来自 pages/cloud.tsx 上新增的 `data-tour`，第 1 步的
 * `nav.pages` 来自 topnav.tsx（沿用「目标」「会话」页的开场方式）。全项目每个
 * data-tour 键必须唯一，`cloud.` 前缀就是为此。
 *
 * 「CLIENTS 接入」README 里有，但当前界面上**没有这个区块**（后端也不再注册
 * cloud_clients / cloud_client_attach 命令），所以那一步故意不带锚点，退化成
 * 居中卡片讲清备份约定；等界面把区块加回来，给它补一个 `cloud.clients` 即可。
 */
export const CLOUD_TOUR: TourStep[] = [
  {
    anchor: 'nav.pages',
    placement: 'bottom',
    title: '这一页是本地改写代理',
    body:
      '客户端直连中转站时，提示词里的敏感词会被云端审查拦下，直接回拒答。云过审在本机起一个代理：客户端把 base_url 指到它，请求先按过云审逻辑改写用户提示词再转发上游，响应回来后判定 / 还原 / 洗白才回传。',
  },
  {
    anchor: 'cloud.server',
    placement: 'right',
    title: '本地服务端：监听地址',
    body:
      '这一块就是代理本体。监听地址默认 127.0.0.1:14649，只在本机监听、外网连不进来；运行中地址栏是锁住的，要改端口得先停服务端。下面「随工具启动」打开后，开工具自动起、退出自动停。',
  },
  {
    anchor: 'cloud.start',
    placement: 'right',
    title: '启动服务端：真正的开关',
    body:
      '点「启动服务端」才开始监听：顶栏徽章变「已启动」，面板角标变「运行中」。「中转站自检」要运行中才可点，它会真发一个探测请求确认上游通；「清零」只清统计数字，不动配置。',
  },
  {
    anchor: 'cloud.stats',
    placement: 'right',
    title: '自检统计：一行看懂链路',
    body:
      '请求 = 进来多少；命中 = 改写命中几处；重试 = 升级重发波次；灰盒 = 疑似被拦；洗白 = 拒答被替换次数；闸门 = 纯问候没走上游；错误 = 链路报错。这行右边徽章是最近一次自检结论。',
  },
  {
    anchor: 'cloud.upstream',
    placement: 'right',
    title: '上游：填自己的网关地址',
    body:
      '填自己网关或中转站的地址（带 /v1 也行）与 API Key。接口形态两种：openai 走自己的网关；tamper-proxy 是服务端过云审接口，请求体包在 request_body 里，规则由服务端执行。',
  },
  {
    anchor: 'cloud.rules',
    placement: 'left',
    title: '改写与判定：规则的来源',
    body:
      '请求侧按 RAW → 洗白 → 令牌化 → 预热 → 续跑 → 意图重述逐级升级，响应侧做 win / soft / policy 判决。洗白内置 31 条、判定内置 8 组，都能在「规则表文件」里自定义增删。',
  },
  {
    anchor: 'cloud.features',
    placement: 'left',
    title: '功能开关：这一组管什么',
    body:
      '从这一行往下：纯问候闸门（纯 hi / 你好 直接回握手，不走上游）、敏感词洗白、目标令牌化（URL / 路径 / IP → 占位符）、上下文预热、多波重试、响应洗白（拒答整段替换）。开关改动即时生效。',
  },
  {
    anchor: 'cloud.log',
    placement: 'left',
    title: '运行日志：出问题先看这里',
    body:
      '只筛出 [cloud] 开头的行：每次请求的波次升级、洗白处数、判决结果和错误都落在这。接上客户端后随便发一句话，能看到「波次 N」和命中条数就说明链路真的在跑；一条都没有，就是流量没进来。',
  },
  {
    // 当前界面没有 CLIENTS 区块，这里刻意不带锚点：宁可退化成居中卡片，
    // 也不要挂到一个不存在的元素上（那样连高亮带文案一起失效）。
    title: '把客户端接到本地代理上',
    body:
      '代理起来后，把 Codex / Claude Code / DSH / ZCode / Cursor 的 base_url 指向上面的地址。原配置不丢：接入先备份成 *.alice-bak-<时间戳>，可一键还原。',
  },
  {
    anchor: 'cloud.save',
    placement: 'bottom',
    title: '保存：原子写盘 + 热更新',
    body:
      '改完上游、开关、规则点这里落盘：先写临时文件再改名，中途断电也不会把配置写坏。运行中的代理当场热更新，下一请求就用新规则，不必重启服务端。按钮灰着 = 当前没有未保存的改动。',
  },
  {
    anchor: 'cloud.reset',
    placement: 'bottom',
    title: '还原默认：只清规则和开关',
    body:
      '点了会把功能开关全部打开、自定义规则清空、重试波次回默认，但上游地址与 API Key 一律保留 —— 手滑了也不用重填密钥。想清统计数字就用服务端面板里的「清零」。',
  },
  {
    title: '云过审主线：四步走完',
    body:
      '填上游地址与 Key → 启动服务端 → 客户端 base_url 指过来 → 回来看统计和日志确认生效。细节忘了随时点右上「使用教程」重看这一页。',
  },
]
